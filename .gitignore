# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/

# Environment files
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# TypeScript
*.tsbuildinfo

# PWA generated
dev-dist/

# Supabase
.supabase/

# Test + coverage output. These were previously committed -- 58 coverage files and a
# directory of Playwright traces and screenshots from an E2E setup that is not installed.
# All of it regenerates from `npm run test:coverage`.
coverage/
test-results/
test-results.json
playwright-report/
playwright/.auth/
playwright_output*.txt
vitest_output*.txt

# Database backups and scratch dumps. Never commit these: they contain real user data
# (names, email addresses). Use `supabase db dump` and keep the output outside the repo.
backup_*.sql
backup-*/
auth_test_out.txt
supabase_schema.txt
.agent/scaling-remaining-items.md

# Local database dumps taken before destructive operations. Never commit these:
# they contain real user rows and auth data.
backups/
